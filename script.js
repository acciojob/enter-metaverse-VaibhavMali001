const button = document.getElementById("enterBtn");
const heading = document.getElementById("status");

button.addEventListener("click", () => {
  heading.innerHTML = "<h1>Entered Metaverse</h1>"; // Change the text to <h2> tag
});